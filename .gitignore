# Downloaded / derived data is reproducible from the scripts — keep it out of git.
data/cache/

# Intermediate flood-fill frames — the committed deliverable is the GIF (output/quabbin_floodfill.gif)
output/floodfill_frames/

# R session cruft
.Rhistory
.RData
.Rproj.user/

# imprint-stage cache-guard sidecars (regenerated; the committed manifest is map/data/imprints.json)
map/data/.bounds_*

# Keep bundled source data (data/*.csv) and rendered output/ committed.
